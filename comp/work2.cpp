#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;

        vector<int> top(n);
        vector<int> bottom(n);   

        for(int i = 0; i < n; i++) top[i]
        
        int min_colors = n;

        if(n % 2 == 0){
            //can check adjacents

        }

    }

    return 0;
}
